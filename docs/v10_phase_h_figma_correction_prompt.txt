AHDASH | 11 — V10 CATEGORY C FIGMA CORRECTION PROMPT

Edit only the existing locked Figma file `1tbYuMwiC8b9vCj12TzbAA`. Keep exactly one page named `AHDASH | 11 — FINAL SCREENS`, the current canonical screen/variant inventory, the existing one-row horizontal order, every top-level frame at Y=0, and approximately 80px gaps. Do not add pages, duplicate frames, restore Archive/Foundation/Review pages, restore Landscape screens, reorder frames, or modify unrelated approved frames. Modify only the 18 Category C frames below.

Preserve the V10 visual language: warm paper, controlled green, selective gold, Arabic-first RTL, Portrait-first, and Thmanyah Sans direction. Do not broadly redesign. Never add XP, Coins, Wallet, fake leaderboard ratings, fake team statistics, fake presence/online status, fake saved games, hardcoded Premium prices/discounts, unsupported Dark Mode, or Power Saving. Online 30–32 stays deferred. Auth must reflect real provider/configuration behavior. Football/category imagery must be rights-safe. Any illustrative data must be labelled DESIGN FIXTURE and must never be specified as runtime fallback.

1) Screen 03, Sign In, Compact 360x800. Current reference: `03_sign_in_compact_360x800`; node ID unknown. Problem: the static card compresses footer/legal content and fails supported text scaling. Approved truth: real configured providers, email/guest actions, validation, legal/footer content, keyboard reachability and 44px+ targets. Correction: make this existing frame vertically scroll-safe and specify reachable focus/validation at 1.3x text. Must not change/remove provider states, guest/email actions, validation, legal copy, safe areas or minimum targets.

2) Screen 04, Create Account, Compact 360x800. Current reference: `04_create_account_compact_360x800`; node ID unknown. Problem: three required fields, providers, validation and footer/legal cannot coexist in its fixed compressed geometry. Approved truth: username/email/password validation, real provider states and legal/footer are mandatory. Correction: make the existing frame scrollable and keyboard-safe with reachable focus/validation. Must not remove any field, provider truth, legal content, safe area or accessibility target.

3) Screen 06, Category Selection, Search Keyboard 390x844. Current reference: `06_category_search_keyboard_390x844`; node ID unknown. Problem: illustrative media/progress conflict with the required six provider-backed categories. Approved truth: six selected/provider-backed categories, reachable search, real selection state and rights-safe fallback media. Correction: correct count/state to six, use approved media/fallback, and keep focused search reachable above the keyboard. Must not change search behavior, six-category requirement, grid/gutters, CTA or keyboard usability.

4) Screen 06, Category Selection, Compact 360x800. Current reference: `06_category_selection_compact_360x800`; node ID unknown. Problem: labels, imagery and count are illustrative/inconsistent. Approved truth: six provider-backed categories with rights-safe fallback. Correction: show a coherent six-category state with approved imagery while preserving the existing compact grid/gutters. Must not reduce the requirement, invent unavailable categories, change search/CTA, or require copyrighted imagery.

5) Screen 06, Category Selection, Primary 390x844. Current reference: `06_category_selection_primary_390x844`; node `126:553`. Problem: progress says 2/4 and imagery is illustrative although six categories are required. Approved truth: six provider-backed categories and rights-safe media. Correction: change progress/content to six and replace media with approved assets/fallback, retaining grid, gutters and CTA hierarchy. Must not change search, selection behavior, category requirement or unrelated geometry.

6) Screen 07, Category Detail, Compact 360x800. Current reference: `07_category_detail_compact_360x800`; node ID unknown. Problem: a hardcoded league, description and photograph imply fixed runtime truth and unverified rights. Approved truth: provider-backed variable name/description/count and rights-safe fallback. Correction: define variable content bounds, use approved fallback, and label any sample DESIGN FIXTURE. Must not alter CTA hierarchy, fabricate league data, or require the sample photograph.

7) Screen 07, Category Detail, Primary 390x844. Current reference: `07_category_detail_primary_390x844`; node `126:622`. Problem: the same hardcoded copy/photo conflicts with remote data and rights-safe behavior. Approved truth: variable provider fields and approved fallback. Correction: replace runtime implication with bounded variable fields/fallback while retaining Primary hierarchy. Must not change CTA hierarchy, remote-data truth or unrelated geometry.

8) Screen 08, Team Setup, Compact 360x800. Current reference: `08_team_setup_compact_360x800`; node ID unknown. Problem: names-only setup omits player summary, colour choice and optional splitter. Approved truth: real team/player/colour state, validation, and optional—not mandatory—Team Splitter. Correction: add compact supported player summary, colour controls and optional splitter entry. Must not invent statistics, force splitting, remove validation or shrink targets.

9) Screen 08, Team Setup, Primary 390x844. Current reference: `08_team_setup_primary_390x844`; node `126:661`. Problem: names-only setup omits supported player/colour state. Approved truth: two real teams with names, players, colours, validation and optional splitter. Correction: incorporate those states into the current hierarchy. Must not add fake statistics, force splitter, change validation or touch unrelated frames.

10) Screen 10, Helpers, Compact 360x800. Current reference: `10_helpers_compact_360x800`; node ID unknown. Problem: labels/descriptions/icons use stale semantics. Approved truth: exactly `two_chances`, `call_friend`, `risk`, `bench`, `pass`; `call_friend` is an internal timer, never phone/contacts; three helpers per team. Correction: replace stale copy/pictograms with these definitions within the established compact layout. Must not add/remove helpers, imply device permissions, or change the limit.

11) Screen 10, Helpers, Primary 390x844. Current reference: `10_helpers_primary_390x844`; node `126:824`. Problem: stale helper semantics/iconography. Approved truth: the exact five identifiers above, internal-timer `call_friend`, three per team. Correction: update labels, descriptions and icons only. Must not change card count, selection limit, team ownership or introduce contact behavior.

12) Screen 11, Ready, Compact 360x800. Current reference: `11_ready_compact_360x800`; node ID unknown. Problem: stale category/helper summary. Approved truth: six selected categories, two real teams, three approved helpers each, truthful setup and no fake countdown. Correction: rebuild current compact summary content from that contract and retain the real start action. Must not invent countdown, names, scores, categories, helpers or statistics.

13) Screen 11, Ready, Primary 390x844. Current reference: `11_ready_primary_390x844`; node `126:889`. Problem: stale summary/helper iconography. Approved truth: six categories, two real teams, three approved helpers each, no fake countdown. Correction: synchronize content/pictograms while retaining Primary hierarchy/start action. Must not add invented setup data, scores, countdown or statistics.

14) Screen 18, Saved Games, Primary 390x844. Current reference: `18_saved_games_primary_390x844`; node `126:1398`. Problem: two illustrative sessions look like fixed content. Approved truth: loading, real persisted sessions, empty/error; resume uses restoration resolver. Correction: specify variable real-session cards plus truthful empty/error states; label any sample DESIGN FIXTURE. Must not hardcode sessions/counts/progress or change resume behavior.

15) Screen 29, Solo Setup, Primary 390x844. Current reference: `29_solo_setup_primary_390x844`; node `126:1892`. Problem: it advertises question-count/difficulty/category configuration not currently published ready. Approved truth: Solo is LIMITED and may have no published ready category. Correction: replace configuration with truthful limited/empty state and supported navigation/action. Must not add unavailable modes, invented content/scores, XP or Coins.

16) Screen 33, Team Challenge, Primary 390x844. Current reference: `33_team_challenge_primary_390x844`; node `126:2073`. Problem: invented opponents, members/settings appear as runtime truth. Approved truth: server-derived data with loading, unavailable/failure and variable real-data states. Correction: show truthful unavailable/loading or label populated content DESIGN FIXTURE with variable bounds. Must not fabricate opponent, presence, rating, queue, members or statistics.

17) Screen 35, Friends, Primary 390x844. Current reference: `35_friends_primary_390x844`; node `126:2205`. Problem: illustrative avatars/green dots imply fake presence and unsupported challenge claims. Approved truth: real server rows, no inferred presence/last-seen, rights-safe packaged avatar fallback, truthful empty state. Correction: remove presence indicators/unsupported claims, use variable real media/fallback, and specify empty state while retaining backed search/actions. Must not fabricate users, availability, ratings, relationships or challenge capability.

18) Screen 35, Friends, Search Keyboard 390x844. Current reference: `35_friends_search_keyboard_390x844`; node ID unknown. Problem: the same fake avatar/presence content appears in a static keyboard composition. Approved truth: real server rows without inferred presence, packaged fallback, empty/no-results and keyboard-safe search. Correction: remove presence dots/unsupported claims, use variable/fallback avatars, and keep focused search plus results/no-results reachable above keyboard. Must not invent users, online state, ratings/actions or change real search behavior.

Completion check: exactly these 18 existing Category C frames were corrected; no other page, frame, order, top-level Y, gap or inventory changed. Online 30–32 remains deferred. Do not overwrite any Flutter Golden or canonical reference PNG.
