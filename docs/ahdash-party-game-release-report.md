# تقرير إصدار لعبة الجلسة — أحدعش | 11

الحالة النهائية في 2026-08-31: **جاهز للاختبار اليدوي على جهاز Android حقيقي**. اكتملت جولة Party وبُني APK Release موقّع دون تشغيل Emulator أو تثبيت التطبيق، ولم يُنفّذ أي `supabase db push` فعلي.

## المنتج المنفذ

- المسار الكامل: Home ← الأقسام ← الفرق ← المساعدات ← الاستعداد ← لوحة 6×6 ← السؤال ← الكشف ← احتساب النقاط ← النتيجة.
- اختيار ستة أقسام بالضبط؛ أول ثلاثة للفريق الأول والتالية للفريق الثاني، مع البحث والمفضلة والأحدث والعشوائي والمعاينة.
- 36 سؤالًا فريدًا: سؤالان 100 وسؤالان 200 وسؤالان 300 لكل قسم.
- أسماء وألوان الفرق وتقسيم اللاعبين، حفظ الجلسة واستئنافها، سجل الأسئلة، التراجع، وإعادة اللعب مع إبقاء إعدادات الفرق.
- صيغ open answer وmultiple choice وtrue/false وimage مع معالجة آمنة للوسائط المفقودة والنص العربي الطويل.
- مؤقت محفوظ بمرجع زمني، وتعادل اختياري يستخدم سؤالًا احتياطيًا غير مستعمل فقط إذا سمحت إعدادات اللعبة.
- الوصول الأساسي للمحفظة أُزيل من المسار الرئيسي، بينما Premium الشهري/السنوي وRevenueCat بقيا كما هما؛ لا دفع لكل جولة ولا أفضلية تنافسية مدفوعة.

## قواعد المساعدات

- مساعد واحد فقط لكل سؤال، وكل مساعد مختار يُستخدم مرة واحدة في الجلسة.
- `فرصتين`: بعد السؤال.
- `استنجد`: بعد السؤال، ومدة الاتصال الافتراضية 20 ثانية وقابلة للضبط من Admin.
- `مخاطرة`: قبل السؤال، والافتراضي مضاعفة الصحيح مع خصم نقطة عند الخطأ، والقيم قابلة للضبط.
- `على الدكة`: بعد السؤال ويتطلب أسماء اللاعبين.
- `مرّرها`: بعد السؤال، والخصم الافتراضي عند الخطأ قابل للضبط.
- أسماء المساعدات ووصفها وأيقوناتها وتوقيتها وإعداداتها تُدار من Admin وتُلتقط snapshot داخل الجلسة حتى لا تتغير قواعد جولة جارية.

## البيانات وSupabase

- أُعيد استخدام جداول الأسئلة والخيارات والأقسام والسجل والإعدادات القائمة.
- أضيفت metadata اللازمة لـParty، والمفضلة، وكتالوج المساعدات، وإعدادات الجلسة مع RLS.
- الـmigration الوحيدة الجديدة: `supabase/migrations/20260830000100_party_game_content.sql`.
- نجح PostgreSQL parser على 20 migration و124 دالة PL/pgSQL.
- نجح `npx supabase db push --dry-run` وأظهر migration الجديدة فقط؛ لم يحدث أي تغيير فعلي في المشروع البعيد.
- نجح linked lint مع 11 تحذيرًا موجودًا أصلًا في قاعدة البيانات البعيدة. تعذر local lint فقط لأن PostgreSQL/Docker المحلي لم يكن يعمل على `127.0.0.1:54322`.
- حزمة Party الآمنة مخصصة للتدريب المحلي غير التنافسي، ولا تُخزّن إجابات تنافسية في cache العميل.

## Admin والمحتوى

- محرر الأسئلة يدعم Party metadata وحالة النشر وحقوق الوسائط.
- صفحة صحة الأقسام تعرض جاهزية حزمة 6×6.
- إدارة كاملة للمساعدات وإعداداتها، وإعدادات اللعب المجاني الافتراضية بصفر/غير مفعلة.
- الاستيراد يدعم القسم والسؤال والإجابة والبدائل والصعوبة و100/200/300 والصيغة والوسائط والمصدر والشرح والموسم والحالة وحقوق الوسائط.
- حفظ الأسئلة المفتوحة لا ينشئ خيارات وهمية، ومنطق اكتشاف التكرار والتصنيف بقي دون تغيير.

## إثبات الفحوصات

| الفحص | النتيجة |
|---|---|
| Dart format | 152 ملفًا، 0 تغييرات |
| Flutter analyze `--fatal-infos` | ناجح، 0 issues |
| Party engine/controller | 12/12 ناجح |
| Party Goldens | 100/100 ناجح؛ 10 شاشات × 5 أحجام × Light/Dark |
| Flutter full suite | 297/297 ناجح |
| Admin lint/typecheck | ناجح |
| Admin tests | 48/48 ناجح في 10 ملفات |
| Admin production build | ناجح، 17 صفحة static |
| PostgreSQL parser | 20 migrations و124 دالة ناجحة |
| Supabase dry run | ناجح؛ بلا push فعلي |

## APK النهائي

- الحالة: `SUCCESS`.
- المسار: `C:\dev\ahdash11\mobile\build\app\outputs\flutter-apk\app-release.apk`.
- الحجم: `91,574,482 bytes` (`87.33 MiB`).
- النوع: `Release Signed`، وتحقق `apksigner` من توقيع v2.
- الحزمة: `com.ahdash.eleven`.
- الإصدار: `versionCode 1`، `versionName 0.1.0`.
- SHA-256: `CC2ED3B6D3D49A52417E225BEC0628A74BBF769266F7365B9B8C60FC2AD376F8`.
- استُخدم الأمر `flutter build apk --release --dart-define-from-file=.env` مع keystore و`key.properties` الموجودين دون طباعة أسرارهما.
- إعداد Android OAuth موجود، وSHA-1 مطابق، و`default_web_client_id` موجود داخل Release.
- موارد Firebase/FCM (`google_app_id` و`gcm_defaultSenderId`) موجودة داخل Release.

## المتبقي للاختبار اليدوي

نجاح الفحوصات والبناء لا يثبت التشغيل الفعلي على الهاتف. تبقى عمليات إنشاء الحساب وتسجيل الدخول وGoogle Sign-In وFCM والإشعارات وSupabase المباشر والصور الديناميكية وPremium والاستعادة و1v1/2v2 واللعب ضد النظام وoffline والأداء والتنقل وRTL وLight/Dark تحت اختبار الجهاز الحقيقي. لا توجد مطالبة بأن هذه التدفقات نجحت runtime قبل أن يختبرها صاحب المشروع.
